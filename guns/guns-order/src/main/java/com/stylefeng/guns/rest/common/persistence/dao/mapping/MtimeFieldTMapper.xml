<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.stylefeng.guns.rest.common.persistence.dao.MtimeFieldTMapper">

    <select id="selectHallIdById" resultType="int">
        select hall_id from mtime_field_t where uuid = #{fieldId}
    </select>
    <select id="selectCinemaIdById" resultType="int">
        select cinema_id from mtime_field_t where uuid = #{fieldId}
    </select>
    <select id="selectFilmIdById" resultType="int">
        select film_id from mtime_field_t where uuid = #{fieldId}
    </select>
    <select id="selectPriceById" resultType="java.lang.Double">
        select price from mtime_field_t where uuid = #{fieldId}
    </select>

    <select id="selectFieldBeginTimeById" resultType="string">
        select begin_time from mtime_field_t where uuid = #{fieldId}
    </select>
</mapper>
